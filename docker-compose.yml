services:
  postgres:
    image: postgres:17
    environment:
      - POSTGRES_USER=denis
      - POSTGRES_PASSWORD=denis
      - POSTGRES_DB=denis
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
