services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_USER=denis
      - POSTGRES_PASSWORD=denis
      - POSTGRES_DB=denis
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
